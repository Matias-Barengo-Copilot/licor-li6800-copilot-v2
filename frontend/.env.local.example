# Read-only Neon DB connection (never write from PIQ)
DATABASE_URL_READONLY=postgres://user:password@host/dbname?sslmode=require

# JWT secret — must match Smart-Attendance if sharing sessions
JWT_SECRET=change-me-to-a-long-random-secret

# OpenAI — used by /api/chat (GPT-4o)
OPENAI_API_KEY=sk-...
